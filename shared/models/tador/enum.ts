export enum ActionType {
    idle = 0,
    register = 1,
    readAll = 2,
    writeAll = 3,
    read = 4,
    write = 5,
    status = 6,
    nameOrder,
    writeAllProgress = 10,
    readAllProgress = 11,
    readProgress = 12,
    writeProgress = 13,
}

export enum PanelType {
    MP = 'MP',
}
